syntax = "proto3";

option csharp_namespace = "Moderation.Grpc";

service ModerationService {
  rpc CheckUser (CheckUserRequest) returns (CheckUserResponse);
}

message CheckUserRequest {
  string userId = 1;
}

message CheckUserResponse {
  bool isBlocked = 1;
}

